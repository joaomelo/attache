export function researchStake(payload, dependencies) {

  // grab every term
  // search it

  // grab every site
  // check position against term searches

  // mount research report

  // notify stake subscriptions there is a new report
}
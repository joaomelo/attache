describe.skip('Page Rank Evolution Metrics', () => {
  // return a metrics object with { page, term, start, end, data }
  // data is a array object ordered by date with object in the shape
  // { when, position }

  // if the period is empty will return an empty array in data

  test('placeholder', () => {});
});
